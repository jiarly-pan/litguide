[project]
name = "litguide"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["click>=8.0", "rich>=13.0"]

[project.scripts]
litguide = "litguide.cli:main"
litguide-gui = "litguide.gui:main"

[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"
